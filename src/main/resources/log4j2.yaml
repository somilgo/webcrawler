Configuration:
  status: warn

  Appenders:
    File:
      PatternLayout:
        pattern: '%d{yyyy-MMM-dd HH:mm:ss a} [%t] %-5level %logger{36} - %msg%n'
      name: File
      fileName: app.log

  Loggers:
    Root:
      level: info
      AppenderRef:
        ref: File